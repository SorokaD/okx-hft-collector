route:
  group_by: ['alertname']
  group_wait: 0s
  group_interval: 0s
  repeat_interval: 1h
  receiver: 'default'

receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://alert-webhook:5001/webhook'
        send_resolved: true
